spring:
  datasource:
    url: jdbc:mysql://localhost:3306/japandream?useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: ZYSJT

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

security:
  jwt:
    issuer: "secure-service"
    secret: "${JWT_SECRET}"